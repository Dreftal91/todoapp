extends layout

block content
    form(action="/add" method= "post")
        label(for="description") Description
        input(type="text" id="description" name="dscription")
        button(type="submit") Add Item